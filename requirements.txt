# Core dependencies
gtfs-kit>=6.0.0  # GTFS parsing and validation
pandas>=2.0.0  # Data manipulation
numpy>=1.24.0  # Numerical operations
pyyaml>=6.0  # Config file parsing

# Geospatial
osmnx>=1.9.0  # Street network data
geopandas>=0.14.0  # Geospatial data operations
shapely>=2.0.0  # Geometric operations
pyproj>=3.6.0  # Coordinate transformations
rtree>=1.0.0  # Spatial indexing

# Routing
r5py>=1.0.0  # Fast transit routing with GTFS (requires Java)
osmium>=3.6.0  # Crop OSM PBF extracts (src/crop_osm.py)

# Geocoding
geopy>=2.4.0  # Address to coordinate conversion

# Visualization
plotly>=5.18.0  # Interactive plots and maps
contourpy>=1.3.0  # Filled contour bands for the heat map (ships with matplotlib)
contextily>=1.7.0  # OSM basemap tiles for static map rendering
folium>=0.15.0  # Alternative mapping library
matplotlib>=3.8.0  # Static plots

# Web framework
flask>=3.0.0  # Web server
flask-cors>=4.0.0  # CORS support

# LLM integration
anthropic>=0.18.0  # Claude API client

# Utilities
tqdm>=4.66.0  # Progress bars
requests>=2.31.0  # HTTP requests
